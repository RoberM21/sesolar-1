// import { fromJS } from 'immutable';
// import { makeSelectDetalleSeguimientoDomain } from '../selectors';

// const selector = makeSelectDetalleSeguimientoDomain();

describe('makeSelectDetalleSeguimientoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
