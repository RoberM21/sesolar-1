// import { fromJS } from 'immutable';
// import { makeSelectNuevoClienteDomain } from '../selectors';

// const selector = makeSelectNuevoClienteDomain();

describe('makeSelectNuevoClienteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
