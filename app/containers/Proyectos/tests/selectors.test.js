// import { fromJS } from 'immutable';
// import { makeSelectProyectosDomain } from '../selectors';

// const selector = makeSelectProyectosDomain();

describe('makeSelectProyectosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
