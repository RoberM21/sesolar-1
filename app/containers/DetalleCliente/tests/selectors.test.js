// import { fromJS } from 'immutable';
// import { makeSelectDetalleClienteDomain } from '../selectors';

// const selector = makeSelectDetalleClienteDomain();

describe('makeSelectDetalleClienteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
