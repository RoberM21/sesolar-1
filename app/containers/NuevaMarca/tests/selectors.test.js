// import { fromJS } from 'immutable';
// import { makeSelectNuevaMarcaDomain } from '../selectors';

// const selector = makeSelectNuevaMarcaDomain();

describe('makeSelectNuevaMarcaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
