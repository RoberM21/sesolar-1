// import { fromJS } from 'immutable';
// import { makeSelectAgregarProyectoDomain } from '../selectors';

// const selector = makeSelectAgregarProyectoDomain();

describe('makeSelectAgregarProyectoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
