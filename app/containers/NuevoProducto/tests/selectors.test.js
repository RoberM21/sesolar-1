// import { fromJS } from 'immutable';
// import { makeSelectNuevoProductoDomain } from '../selectors';

// const selector = makeSelectNuevoProductoDomain();

describe('makeSelectNuevoProductoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
