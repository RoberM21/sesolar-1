// import { fromJS } from 'immutable';
// import { makeSelectMarcasDomain } from '../selectors';

// const selector = makeSelectMarcasDomain();

describe('makeSelectMarcasDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
