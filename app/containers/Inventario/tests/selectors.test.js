// import { fromJS } from 'immutable';
// import { makeSelectInventarioDomain } from '../selectors';

// const selector = makeSelectInventarioDomain();

describe('makeSelectInventarioDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
