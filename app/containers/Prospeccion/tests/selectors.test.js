// import { fromJS } from 'immutable';
// import { makeSelectProspeccionDomain } from '../selectors';

// const selector = makeSelectInventarioDomain();

describe('makeSelectProspeccionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
