// import { fromJS } from 'immutable';
// import { makeSelectClientesDomain } from '../selectors';

// const selector = makeSelectClientesDomain();

describe('makeSelectClientesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
