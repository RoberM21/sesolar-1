// import { fromJS } from 'immutable';
// import { makeSelectEditarMarcaDomain } from '../selectors';

// const selector = makeSelectEditarMarcaDomain();

describe('makeSelectEditarMarcaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
