// import { fromJS } from 'immutable';
// import { makeSelectCotizacionesDomain } from '../selectors';

// const selector = makeSelectCotizacionesDomain();

describe('makeSelectCotizacionesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
