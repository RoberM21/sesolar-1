// import { fromJS } from 'immutable';
// import { makeSelectAgregarProspectoDomain } from '../selectors';

// const selector = makeSelectAgregarProspectoDomain();

describe('makeSelectAgregarProspectoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
