// import { fromJS } from 'immutable';
// import { makeSelectEditarClienteDomain } from '../selectors';

// const selector = makeSelectEditarClienteDomain();

describe('makeSelectEditarClienteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
