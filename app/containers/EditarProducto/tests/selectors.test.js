// import { fromJS } from 'immutable';
// import { makeSelectEditarProductoDomain } from '../selectors';

// const selector = makeSelectEditarProductoDomain();

describe('makeSelectEditarProductoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
