// import { fromJS } from 'immutable';
// import { makeSelectProductosDomain } from '../selectors';

// const selector = makeSelectProductosDomain();

describe('makeSelectProductosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
